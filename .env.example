# Puerto en el que corre el servidor Express
PORT=3000

# Cadena de conexion a MongoDB Atlas (o local)
# Reemplazar <usuario>, <password> y <cluster> por los datos reales
MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/sn_caso_uso_2
